import ProgramsSettings from"./programs-settings";import{createAdminClient}from"@/lib/supabase/admin";import{formatResultDateLabel}from"@/lib/results/result-date-label";
export default async function ProgramsPage(){const admin=createAdminClient();const[programs,links]=await Promise.all([admin.from("learning_programs").select("id,name,created_at").eq("is_active",true).order("name"),admin.from("student_learning_programs").select("program_id")]);if(programs.error||links.error)throw new Error("Не удалось загрузить программы.");const counts=new Map<string,number>();for(const link of links.data)counts.set(link.program_id,(counts.get(link.program_id)??0)+1);return <ProgramsSettings programs={programs.data.map(item=>({id:item.id,name:item.name,createdLabel:formatResultDateLabel(item.created_at),studentCount:counts.get(item.id)??0}))}/>}
