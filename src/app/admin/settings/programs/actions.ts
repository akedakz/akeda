"use server";
import {revalidatePath}from"next/cache";import{getCurrentProfile}from"@/lib/auth/get-current-profile";import{createAdminClient}from"@/lib/supabase/admin";
async function admin(){const current=await getCurrentProfile();return current?.profile?.role==="ADMIN"?createAdminClient():null}function refresh(){revalidatePath("/admin/settings");revalidatePath("/admin/settings/programs")}
export async function createProgram(data:FormData){const client=await admin();if(!client)return{ok:false,message:"Недостаточно прав."};const name=String(data.get("name")??"").trim();if(!name||name.length>100)return{ok:false,message:"Введите название до 100 символов."};const result=await client.from("learning_programs").insert({name});if(result.error)return{ok:false,message:result.error.code==="23505"?"Активная программа с таким названием уже существует.":"Не удалось создать программу."};refresh();return{ok:true,message:"Программа создана."}}
export async function archiveProgram(id:string){const client=await admin();if(!client)return{ok:false,message:"Недостаточно прав."};const result=await client.from("learning_programs").update({is_active:false,updated_at:new Date().toISOString()}).eq("id",id).eq("is_active",true);if(result.error)return{ok:false,message:"Не удалось архивировать программу."};refresh();return{ok:true,message:"Программа архивирована."}}
